graph = { soi ~ metric_name ~ eoi }

metric_name = ${ metric_name_inner | "'" ~ metric_name_inner ~ "'" | "\"" ~ metric_name_inner ~ "\"" }
metric_name_inner = @{ ( 'a'..'z' | 'A'..'Z' | '0'..'9' | "-" | "_" | "." | "*" )+ }

whitespace = _{ " " | "\t" | "\r" | "\n" }
